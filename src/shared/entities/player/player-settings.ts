import { PlayerKeySettings } from "./player-key-settings"

class PlayerSettings
{
    playerKeys: PlayerKeySettings
    constructor(playerKeys: PlayerKeySettings) 
    { 
        this.playerKeys = playerKeys
    } 
}
export { PlayerSettings }